namespace SearchDemo.Models
{
    /// <summary>
    /// A published entity. Used to tie together other classes so we can reference them for searching.
    /// </summary>
    public class PublishedEntity
    {
        /// <summary>
        /// Gets or sets the id.
        /// </summary>
        public int Id { get; set; }
    }
}
